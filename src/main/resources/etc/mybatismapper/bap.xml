<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bap">

	<cache />

	<insert id="createBap" parameterType="org.uario.seaworkengine.model.Bap">
		INSERT INTO ship (id_ship,
		shit_no, volume, crane, date_request)
		VALUES
		(#{id_ship}, #{shit_no},
		#{volume}, #{crane},#{date_request});
	</insert>

	<delete id="deleteBap" parameterType="Integer">
		delete from
		bap
		where
		id =
		#{id}
	</delete>

	<select id="loadBap" parameterType="Integer"
		resultType="org.uario.seaworkengine.model.Bap">
		SELECT b.id,
		b.id_ship,
		b.shit_no,
		b.volume,
		b.crane,
		b.date_request,
		s.name as ship_name
		FROM
		bap b, ship s where b.id_ship = s.id
		id = #{id};
	</select>



	<select id="loadAllBap" resultType="org.uario.seaworkengine.model.Bap">
		SELECT b.id,
		b.id_ship,
		b.shit_no,
		b.volume,
		b.crane,
		b.date_request,
		s.name as ship_name
		FROM
		bap b, ship s where b.id_ship = s.id
	</select>

	<update id="updateShip" parameterType="org.uario.seaworkengine.model.Bap">
		UPDATE bap SET
		id_ship =
		#{id_ship},
		shit_no = #{shit_no},
		volume = #{volume},
		crane = #{crane},
		date_request = #{date_request}
		WHERE id = #{id}
	</update>




</mapper>