<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="statistics">

	<cache-ref namespace="schedule" />



	<select id="listSchedule" resultType="org.uario.seaworkengine.model.Schedule"
		parameterType="map">

		select s.id,
		u.id as user,
		s.shift,
		s.date_schedule,
		concat(u.lastname, '
		', u.firstname) AS name_user,
		s.editor,
		s.controller
		from
		user u,
		schedule
		s
		where (u.id =
		s.user)

		<if test="date_from != null and date_to != null">
			and (s.date_schedule <![CDATA[>=]]>
			#{date_from} and s.date_schedule
			<![CDATA[<=]]>
			#{date_to})
		</if>

		<if test="my_full_text_search != null">
			and (u.firstname like CONCAT('%', #{my_full_text_search},
			'%') or
			u.lastname like CONCAT('%', #{my_full_text_search}, '%') or
			u.employee_identification = #{my_full_text_search})
		</if>

		<if test="date_from != null and date_to != null">
			and (s.date_schedule <![CDATA[>=]]>
			#{date_from} and s.date_schedule
			<![CDATA[<=]]>
			#{date_to})
		</if>

		<if test="shift_number != null">
			and (s.shift = #{shift_number})
		</if>

		order by u.lastname asc, s.date_schedule
	</select>




	<select id="overviewFinalSchedule" resultType="org.uario.seaworkengine.model.DetailFinalSchedule"
		parameterType="map">
		select
		concat(u.lastname, ' ', u.firstname) as user,
		dfs.id,
		dfs.id_schedule,
		dfs.shift,
		s.shift as shift_type,
		dfs.task,
		dfs.time,
		dfs.time_from,
		dfs.time_to,
		s.date_schedule
		from
		user u,
		schedule s left
		outer join detailfinalschedule dfs on (dfs.id_schedule = s.id)
		where
		s.user = u.id

		<if test="my_full_text_search != null">
			and (u.firstname like CONCAT('%', #{my_full_text_search},
			'%') or
			u.lastname like CONCAT('%', #{my_full_text_search}, '%') or
			u.employee_identification = #{my_full_text_search})
		</if>

		<if test="shift_number != null">
			and (dfs.shift = #{shift_number})
		</if>

		<if test="date_from != null and date_to != null">
			and (s.date_schedule <![CDATA[>=]]>
			#{date_from} and s.date_schedule
			<![CDATA[<=]]>
			#{date_to})
		</if>

		<if test="shift_type != null">
			and (s.shift = #{shift_type})
		</if>

		order by u.lastname, s.date_schedule, dfs.shift asc

	</select>

	<select id="overviewInitalSchedule" resultType="org.uario.seaworkengine.model.DetailInitialSchedule"
		parameterType="map">
		select
		concat(u.lastname, ' ', u.firstname) as user,
		dfs.id,
		dfs.id_schedule,
		dfs.shift,
		s.shift as shift_type,
		dfs.task,
		dfs.time,
		s.date_schedule
		from
		user u,
		schedule s left outer join
		detailinitialschedule dfs on (dfs.id_schedule = s.id)
		where
		s.user =
		u.id

		<if test="my_full_text_search != null">
			and (u.firstname like CONCAT('%', #{my_full_text_search},
			'%') or
			u.lastname like CONCAT('%', #{my_full_text_search}, '%') or
			u.employee_identification = #{my_full_text_search})
		</if>

		<if test="shift_number != null">
			and (dfs.shift = #{shift_number})
		</if>

		<if test="shift_type != null">
			and (s.shift = #{shift_type})
		</if>

		order by u.lastname, s.date_schedule, dfs.shift asc

	</select>





	<select id="datesBreak" resultType="java.util.Date"
		parameterType="map">
		select
		max(d.date_schedule) as date_break
		from
		schedule d,
		usershift s
		where
		d.date_schedule <![CDATA[>=]]>
		#{date_schedule_from}
		and d.date_schedule <![CDATA[<=]]>
		#{date_schedule_to}
		and s.break_shift = 1
		and d.shift = s.id and
		d.id_user = #{id_user}
	</select>


	<select id="timeWorkedProgram" resultType="int" parameterType="map">
		select
		sum(d_1.time)
		from
		schedule s_1,
		detailinitialschedule d_1
		where
		s_1.user = #{id_user}
		and s_1.id =
		d_1.id_schedule
		and s_1.date_schedule <![CDATA[<=]]>
		#{date_schedule_to}
		and s_1.date_schedule <![CDATA[>=]]>
		#{date_schedule_from}
	</select>

	<select id="timeWorkedReviewd" resultType="int" parameterType="map">
		select
		sum(d_1.time)
		from
		schedule s_1,
		detailfinalschedule d_1
		where
		s_1.user = #{id_user}
		and s_1.id =
		d_1.id_schedule
		and s_1.date_schedule <![CDATA[<=]]>
		#{date_schedule_to}
		and s_1.date_schedule <![CDATA[>=]]>
		#{date_schedule_from}
	</select>


	<select id="dateAtWork" resultType="java.util.Date"
		parameterType="map">
		select
		s.date_schedule
		from
		schedule s,
		usershift us
		where
		s.date_schedule <![CDATA[<=]]>
		#{date_schedule_to}
		and s.date_schedule <![CDATA[>=]]>
		#{date_schedule_from}
		and s.user = #{id_user}
		and s.shift = us.id
		and
		us.presence = 1
		order by s.date_schedule asc
	</select>









	<select id="selectRateShiftByUserProgram" resultType="org.uario.seaworkengine.statistics.RateShift"
		parameterType="map">
		select
		d_1.shift,
		sum(d_1.time) / (select
		sum(d_2.time) as global_sum
		from
		schedule s_2,
		detailinitialschedule d_2
		where
		s_2.user = #{id_user}
		and
		s_2.id =
		d_2.id_schedule
		and (s_2.date_schedule <![CDATA[<=]]>
		#{date_schedule} and s_2.date_schedule >= #{date_from})) * 100 as rate
		from
		schedule s_1,
		detailinitialschedule
		d_1
		where
		s_1.user = #{id_user}
		and s_1.id = d_1.id_schedule
		and
		(s_1.date_schedule <![CDATA[<=]]>
		#{date_schedule} and s_1.date_schedule >= #{date_from})
		group by
		d_1.shift
		order by rate asc

	</select>

	<select id="selectRateShiftByUserReviewd" resultType="org.uario.seaworkengine.statistics.RateShift"
		parameterType="map">
		select
		d_1.shift,
		sum(d_1.time) / (select
		sum(d_2.time) as global_sum
		from
		schedule s_2,
		detailfinalschedule d_2
		where
		s_2.user = #{id_user}
		and
		s_2.id =
		d_2.id_schedule
		and (s_2.date_schedule <![CDATA[<=]]>
		#{date_schedule} and s_2.date_schedule >= #{date_from})) * 100 as rate
		from
		schedule s_1,
		detailfinalschedule
		d_1
		where
		s_1.user = #{id_user}
		and
		s_1.id = d_1.id_schedule
		and
		(s_1.date_schedule <![CDATA[<=]]>
		#{date_schedule} and s_1.date_schedule >= #{date_from})
		group by
		d_1.shift
		order by rate asc

	</select>

	<select id="selectPercentageSunday" resultType="double"
		parameterType="map">


		select
		count(distinct ds.date_schedule) / (select
		count(distinct dd.date_schedule)
		from
		detailfinalschedule dfs_1,
		schedule dd
		where
		dfs_1.id_schedule = dd.id
		and DAYOFWEEK(dd.date_schedule) = 1
		and dd.date_schedule <![CDATA[>=]]>
		#{date_from}) * 100 as perc
		from
		schedule ds,
		usershift us,
		detailfinalschedule dfs
		where
		dfs.id_schedule = ds.id
		and ds.shift = us.id
		and us.presence = 1
		and DAYOFWEEK(ds.date_schedule) = 1
		and ds.user = #{id_user}
		and ds.date_schedule <![CDATA[>=]]>
		#{date_from}

	</select>

	<select id="selectPercentageSundayAndHoliday" resultType="double">
		select
		count(distinct ss.date_schedule) / (select
		count(distinct s.date_schedule)
		from
		schedule s,
		detailfinalschedule dfs
		where
		s.id = dfs.id_schedule
		and s.date_schedule >= #{date_from}
		and (date_format(s.date_schedule, '%m-%d') in (#{days_hol})
		or DAYOFWEEK(s.date_schedule) = 1)) * 100 as perc
		from
		schedule ss,
		usershift uss,
		detailfinalschedule dfs
		where
		dfs.id_schedule = ss.id
		and ss.date_schedule >= #{date_from}
		and ss.user = #{id_user}
		and ss.shift = uss.id
		and uss.presence = 1
		and (date_format(ss.date_schedule, '%m-%d') in (#{days_hol})
		or DAYOFWEEK(ss.date_schedule) = 1)

	</select>



</mapper>